# Construcción y ejecución de aplicaciones con Spring Boot

Una vez tenemos implementada nuestra aplicación —aunque nuestro ejemplo no esté completamente terminado— debemos construir los ficheros **entregables** para que la aplicación pueda ser instalada y ejecutada en su destino.

---

## 1. Gestión de dependencias y construcción

Al crear el proyecto con **Spring Initializr**, podemos elegir entre **Maven** o **Gradle** para la gestión de dependencias.  
Estas herramientas no solo gestionan dependencias, sino que también permiten **construir los ficheros JAR o WAR** de la aplicación.

### 1.1 JAR vs WAR

| Tipo de archivo | Uso principal | Notas |
|-----------------|---------------|-------|
| **WAR**         | Despliegue en servidores web tradicionales | Necesita un servidor externo |
| **JAR autoejecutable** | Aplicaciones Spring Boot modernas | Contiene todo lo necesario para ejecutarse sin servidor externo |

> 💡 **Tip:** Spring Boot recomienda usar JARs autoejecutables por su simplicidad y portabilidad.

---

### 1.2 Construcción del proyecto con Maven

Para construir la aplicación con Maven:

```bash
mvn package
